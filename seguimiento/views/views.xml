<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="seguimiento.list">
      <field name="name">seguimiento list</field>
      <field name="model">seguimiento.seguimiento</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="seguimiento.action_window">
      <field name="name">seguimiento window</field>
      <field name="res_model">seguimiento.seguimiento</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="seguimiento.action_server">
      <field name="name">seguimiento server</field>
      <field name="model_id" ref="model_seguimiento_seguimiento"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="seguimiento" id="seguimiento.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="seguimiento.menu_1" parent="seguimiento.menu_root"/>
    <menuitem name="Menu 2" id="seguimiento.menu_2" parent="seguimiento.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="seguimiento.menu_1_list" parent="seguimiento.menu_1"
              action="seguimiento.action_window"/>
    <menuitem name="Server to list" id="seguimiento" parent="seguimiento.menu_2"
              action="seguimiento.action_server"/>
    -->
  </data>
</odoo>